# Main experiment environment (CPU experiments + GPU carry-chain).
# Existing env reused: /ibex/user/songt/conda_envs/nesy  (Python 3.11).
# torch is the CUDA 12.1 build; install it from the PyTorch index if recreating:
#   pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cu121
# The rest install from PyPI. Java 11 (system) is used only for reference.
torch==2.3.1
numpy==2.4.4
scipy==1.17.1
networkx==3.6.1
PySDD==1.0.6
problog==2.2.10
deepproblog==2.0.6
tensorly==0.9.0
matplotlib==3.10.9
pandas==3.0.3
tqdm==4.67.3
scikit-learn==1.8.0
obonet==1.3.0
# Note: the EL++ normalization (task A) does NOT require mowl here -- it is extracted from
# the pre-normalized go.norm dataset by scripts/05b_extract_norm.py. A separate mowl env
# (requirements-mowl.txt) is only needed to re-derive go.norm from go.owl directly.
